- if user_signed_in?
  %li
    = link_to t("web-app-theme.logout", :default => "Logout"), destroy_user_session_path, :method => :delete
- else
  %li
    = link_to t("web-app-theme.logout", :default => "Login"), new_user_session_path      
